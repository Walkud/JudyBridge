apply plugin: 'maven'

// 版本号，下次更新是只需要更改版本号即可
version = ext.VERSION
//发布到组织名称名字，必须填写
group = GROUP
//上面配置后上传至JCenter后的编译路径是这样的： compile 'com.zly.judy:judy-api:1.0.0'
uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri("../repo"))
        }
    }
}